#!/usr/bin/env ruby
require "rubylog"

files = ARGV.empty? ?  Dir['./logic/**/*_logic.rb'] : Dir[*ARGV].map{|f| File.expand_path f }
ARGV.clear


files.each do |x|
  puts x

  require x

  puts
end




